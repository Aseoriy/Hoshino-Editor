<Window x:Class="HoshinoEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Hoshino Editor" Icon="Assets/Hoshino.ico" Width="1280" Height="800" MinWidth="980" MinHeight="640"
        WindowStyle="None" ResizeMode="CanResize" Background="{DynamicResource AppBackground}"
        Loaded="Window_Loaded" PreviewKeyDown="Window_PreviewKeyDown">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>
  <Border BorderBrush="{DynamicResource Line2}" BorderThickness="1">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="46" /><RowDefinition Height="*" /><RowDefinition x:Name="StatusRow" Height="28" /></Grid.RowDefinitions>

      <Grid Grid.Row="0" Background="#B312121B">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
          <Border Width="27" Height="27" Margin="0,0,10,0" CornerRadius="7" ClipToBounds="True"><Image Source="Assets/HoshinoLogo.jpg" Stretch="UniformToFill" /></Border>
          <TextBlock Text="Hoshino" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
          <TextBlock x:Name="DocumentTitle" Text="  /  New project" Foreground="{DynamicResource TextFaint}" FontSize="12" VerticalAlignment="Center" />
        </StackPanel>
        <Border Grid.Column="1" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" />
        <StackPanel Grid.Column="2" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
          <Button Style="{StaticResource CaptionButton}" Content="⚙" Click="Settings_Click" ToolTip="Settings (Ctrl+,)" />
          <Button Style="{StaticResource CaptionButton}" Content="—" Click="Minimize_Click" ToolTip="Minimize" />
          <Button Style="{StaticResource CaptionButton}" Content="□" Click="Maximize_Click" ToolTip="Maximize" />
          <Button x:Name="CloseButton" Style="{StaticResource CaptionButton}" Content="×" Click="Close_Click" ToolTip="Close" />
        </StackPanel>
      </Grid>

      <ContentControl x:Name="WorkspaceHost" Grid.Row="1" />

      <Border x:Name="StatusBar" Grid.Row="2" Background="#B312121B" BorderBrush="{DynamicResource Line1}" BorderThickness="0,1,0,0">
        <Grid Margin="12,0">
          <TextBlock x:Name="StatusText" Text="Ready" FontSize="11" Foreground="{DynamicResource TextFaint}" VerticalAlignment="Center" />
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Ellipse x:Name="StatusDot" Width="6" Height="6" Fill="{DynamicResource Ok}" Margin="0,0,6,0" />
            <TextBlock Text="LOCAL ONLY" Style="{StaticResource SectionLabel}" />
          </StackPanel>
        </Grid>
      </Border>

      <Border x:Name="Toast" Grid.Row="1" Background="#F21A1724" BorderBrush="{DynamicResource AccentLine}" BorderThickness="1"
              CornerRadius="10" Padding="14,10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,18,18"
              Visibility="Collapsed" Panel.ZIndex="20">
        <TextBlock x:Name="ToastText" FontSize="12" MaxWidth="360" TextWrapping="Wrap" />
      </Border>
    </Grid>
  </Border>
</Window>
